Install dependencies:

    Ubuntu: apt-get install python3-docker docker.io tcpdump
    Arch:   pacman -S python-docker-py docker tcpdump gnuplot

Add a user to the docker group.

Then build all containers (~2h):

    ./python/run.py --build-all

To get more help on running tests:

    ./python/run.py --help
